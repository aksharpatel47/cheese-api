/**
 * IUserDto is a DTO (Data Transfer Object) that is used to represent user data
 * returned from the data to the user.
 */
export interface IUserDto {
  id: number;
  name: string;
  email: string;
}
